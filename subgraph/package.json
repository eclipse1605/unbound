{
  "name": "unbound-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for Unbound SparkRegistry contract",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --studio",
    "deploy": "graph deploy --studio unbound",
    "create-local": "graph create --node http://127.0.0.1:8020/ unbound-local",
    "deploy-local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://127.0.0.1:5001 unbound-local"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.53.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
